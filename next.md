Move the 'Sick leave accrual' label to be before the 'Unused sick leave' label, and rearrange their entry fields likewise.

Where the official value is identical to my calculated value, can you change the tooltip visible when hovering to indicate that, instead of sometimes being in red and saying '(official)' and sometimes being in blue and not specifying? For that matter, since the official line is red and the calculated one is blue, can you detect overlap well enough to draw a single line in purple instead?

Given the number of optional fields, would the gui be easier to read if we grouped the sick-leave and annual-raise fields into their own group, and have that collapsed initially? If so, put that just below the current 'Your information' group.

