Generates a simple graph showing estimated pension amount vs retirement date

## Tests

Pure logic in `lib/pension.js` is covered by Node-based tests using the
built-in `node:test` runner (no dependencies). From the repo root:

```
node --test
```

Test files live in `tests/` and are named `*.test.js`.
